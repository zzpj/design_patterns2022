package pl.p.lodz.zzpj.template;

public class HelperMethods {
    public static boolean userExists(String token) {
        return false;
    }
    public static void saveUser(String token) {
        // get some data and save user in local database
    }
}
